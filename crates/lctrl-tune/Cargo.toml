[package]
name = "lctrl-tune"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false

[dependencies]
lctrl-core = { path = "../lctrl-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8.23"
